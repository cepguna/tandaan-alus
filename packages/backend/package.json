{
  "name": "@extension/backend",
  "version": "0.0.1",
  "description": "chrome extension - backend",
  "scripts": {
    "dev": "convex dev",
    "setup": "convex dev --until-success"
  },
  "dependencies": {
    "@extension/env": "workspace:*",
    "@auth/core": "0.37.0",
    "@convex-dev/auth": "^0.0.86",
    "convex": "^1.13.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
